import build_utils

Import(['env'])

sources = [
    'Module.cpp',
    'TimeDelta.cpp'
    ]

build_utils.build_shared_library(env,
                                 'datetime',
                                 sources)
